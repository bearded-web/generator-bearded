language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bearded' ]; then cd .. && eval "mv $currentfolder generator-bearded" && cd generator-bearded; fi
